/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // You can specify the port here as well if needed
  // env: {
  //   PORT: 3001,
  // },
};

module.exports = nextConfig;
